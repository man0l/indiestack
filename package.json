{
  "name": "indiestack",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite build && wrangler d1 migrations apply DB --local && wrangler dev --test-scheduled",
    "deploy": "vite build && node scripts/upload-assets.mjs && wrangler d1 migrations apply DB --remote && wrangler deploy",
    "types": "wrangler types",
    "check": "wrangler types && tsc --noEmit"
  },
  "devDependencies": {
    "@internationalized/date": "^3.12.0",
    "@lucide/svelte": "^1.39.0",
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "@tailwindcss/vite": "^4.3.3",
    "bits-ui": "^2.16.3",
    "clsx": "^2.1.1",
    "lucide-svelte": "^1.0.1",
    "svelte": "^5.57.0",
    "tailwind-merge": "^3.6.0",
    "tailwind-variants": "^3.3.0",
    "tailwindcss": "^4.3.3",
    "tw-animate-css": "^1.4.0",
    "typescript": "^5.9.2",
    "vite": "^6.4.3",
    "wrangler": "^4.34.0"
  },
  "cloudflare": {
    "bindings": {
      "ADMIN_TOKEN": {
        "description": "Optional. Leave blank — open /admin after deploy; a token is generated and shown once to copy. Or paste your own password here."
      }
    }
  },
  "allowScripts": {
    "esbuild@0.28.1": true,
    "workerd@1.20260828.1": true
  }
}
